{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node"
      }
    ],
    "build": {
      "env": {
        "DATABASE_URL": "your-database-connection-url"
      },
      "commands": {
        "build": "npm run prisma:generate && npm run build"
      }
    }
  }
  